package com.jobboard.enums;

/**
 * User roles within the Job Board system.
 * EMPLOYER  — can post jobs and manage applications.
 * CANDIDATE — can browse jobs and submit applications.
 */
public enum Role {
    EMPLOYER,
    CANDIDATE
}
